PORT=8787
CORS_ORIGIN=http://localhost:3000

# Modal GPU endpoint — deploy with: modal deploy modal/tryon_service.py
TRYON_MODAL_URL=https://zeeshandesigns--stylesense-tryon-web.modal.run
TRYON_API_KEY=

# Rate limiting
TRYON_RATE_LIMIT_MAX=20
RATE_LIMIT_MAX=120

# JWT (unused for try-on but required at startup)
JWT_SECRET=dev-only-secret-change-in-prod

# ML admin key (optional — leave empty to allow open retraining)
ML_ADMIN_KEY=
